import React, { useEffect, useState } from "react";

const ButtonConncted = () => {
  const [account, setAccount] = useState("");

  return (
    <div>
      <button>connect</button>
    </div>
  );
};

export default ButtonConncted;
